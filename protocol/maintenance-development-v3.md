# Maintenance development v3 — replay composition

Higher-diversity joint acquisition in v2 succeeds at1024:128/128 train,8/8
new-ticket complete workflows. Its512 checkpoint is64/128 train and4/8 later.
V1's two-ticket512 checkpoint memorizes16/16 but transfers3/8. Retain both.

Use seed101, data2026091501,16 training tickets/rule,128-update alder state.
Run the first three arrivals birch,cedar,dune with128 updates/arrival. Compare
none,incoming,fixed25,fixed50,fixed75,mir50. Same runtime/CE/reset and data access.
Three arrivals isolate replay composition before spending on later recurrences;
fresh evaluation will add recurring birch,cedar,dune after this acquisition prefix.
All source choices and budgets are development. Select strongest fixed ratio by
complete post-update uses, then final completeness, then lower replay fraction.
Compare a256-update duration for that fixed ratio and MIR; add MIR75 if replay
budget rather than ranking plausibly explains a deficit. Bound this development
without requiring a dynamic advantage. Fresh protocol is not yet fixed.

See maintenance-recurring-draft.md for workflow, virtual mechanism, controls and
cost units. Candidate sample is at most16 archive records, selected top half,
reselected every16 updates using copied current AdamW moments. Exact restoration
and virtual-versus-actual next-step equality are checked. All actual arms use fresh
optimizers each arrival. No future-site update or replay before arrival.
